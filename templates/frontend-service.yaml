#
apiVersion: v1
kind: Service
metadata:
  name: frontend-employee
spec:
  selector:
    app: frontend-employee
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 3000
  type: ClusterIP
